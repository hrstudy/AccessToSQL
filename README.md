# Access To SQL
# 程序功能：
---
1. 可以选择使用的Access驱动程序和mdb文件位置；
2. 可手动输入SQL服务器相关配置信息；
3. 检测指定SQL数据库中是否有对应要导入的Access数据库中的表名，如果没有则建立（建立的时候根据选择的映射字段数据类型来建立，如果需要保持ID自增字段的还需要特殊处理），如果有则根据选择是否需要清空原数据库表中内容，同样需要注意根据选择来对ID自增功能处理；
4. 在数据迁移过程中实时显示导入进度和日志记录；
5. 针对大数据量导入的时要有断点继续导入功能，可以选择继续导入或是删除断点记录重新导入；
